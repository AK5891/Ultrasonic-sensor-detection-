# Ultrasonic-sensor-detection-

This is a basic arduino set up that uses an ultrasonic sensor in order to detect objects within a certain range. Once an object is detected, it simply sets off an alarm. This was coded in C++ on Arduino IDE. 
